<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only: Android 9+ (API 28+) blocks plaintext HTTP by default.
  The local backend during development is plain http:// — this config
  allows cleartext traffic to the dev-server hosts only, and only in debug
  builds (android/app/src/debug/), so release builds stay HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
