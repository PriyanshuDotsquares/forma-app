DATABASE_URL=postgresql+asyncpg://forma:forma@localhost:5432/forma
SECRET_KEY=change-me-in-.env
ACCESS_TOKEN_EXPIRE_MINUTES=1440
CORS_ORIGINS=["*"]

# Only needed to run `python -m app.db.ai_seed` (generates new exercises/
# achievements/challenges via the Groq API). Leave unset otherwise.
GROQ_API_KEY=
 